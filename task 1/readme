the vdi file is installed and executes at oracle vm box 
once the oracle vm box is running the ubuntu , the terminal is opened 
'sudo apt install virtual box 'is executed and has been successful 
from the a new terminal , the instructions given were followed and screenshots of the work is uploaded 
the time and date is indicated in all work 
